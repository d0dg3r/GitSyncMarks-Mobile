# Copy to key.properties and fill in your values.
# key.properties is gitignored - never commit it.
#
# Create upload keystore (one-time):
#   keytool -genkey -v -keystore upload-keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias upload
#
# Place upload-keystore.jks outside the repo (e.g. ~/.android/ or project root) and keep it secure.

storePassword=YOUR_STORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
keyAlias=upload
storeFile=../upload-keystore.jks
